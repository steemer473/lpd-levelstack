/** Global top-bar mode for ProductShell / GlassNavigation. */
export type NavVariant =
  | "default"
  | "freeReport"
  | "paidPendingIntake"
  /** Free snapshot view for a customer who already owns Action Roadmap. */
  | "paidOwnerFree"
