# LevelStack — link to the same Supabase project as lpd-redesign (commercial hub).
# Apply migrations: supabase db push (or via Supabase dashboard SQL editor).

# Same project as lpd-redesign (commercial hub)
project_id = "lppmbgqsovtfbpbvjvxi"

[db]
major_version = 15

[api]
enabled = true

# Magic-link / OTP lifetime for free snapshot emails (must match MAGIC_LINK_EXPIRY_SECONDS in app).
[auth.email]
otp_expiry = 86400
